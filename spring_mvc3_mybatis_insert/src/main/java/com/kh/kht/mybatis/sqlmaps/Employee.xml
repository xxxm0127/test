<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Employee">
		
	<insert id="insertemployee" parameterType="com.kh.kht.domain.Employee">
		INSERT INTO TB_EMPLOYEE(emp_id, emp_name, emp_email, emp_tel) 
		VALUES(#{emp_id}, #{emp_name}, #{emp_email}, #{emp_tel})
	</insert>

</mapper>
